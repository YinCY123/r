<article>
http://www.usaelectionpolls.com

<r:code>
doc = htmlParse("http://www.usaelectionpolls.com/2008/colorado.html")
</r:code>
<r:code>
links = doc[ '//table[./tr/td/a[@href="http://www.usaelectionpolls.com/2008/candidates/Barack-Obama.html"]]']
</r:code>
<r:code>
getPollInfo = 
function(table)
{
  kids = xmlChildren(table)
     # discard the Source row at the bottom.
  ans =
    lapply(kids[ - length(kids) ], 
                 function(row) {
                     structure(as.numeric(gsub("%", "", xmlValue(row[[2]]))),
                                names = xmlValue(row[[1]]))
                 })
  structure(unlist(ans), names = sapply(ans, names))
}
</r:code>


outer table
<r:code>
outerTable = xmlParent(xmlParent(xmlParent(links[[1]])))
</r:code>

</article>